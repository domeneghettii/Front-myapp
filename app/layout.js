import "../styles/globals.css";
			export const metadata = {
				title: "Site Lilo e Stitch",
			};

			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
		}
